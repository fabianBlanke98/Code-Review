{
  "name": "mosaic",
  "version": "0.1.0",
  "private": true,
  "description": "One shared film a group films together.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "lan": "node apps/lan/server.mjs",
    "test": "npm run test --workspace @mosaic/montage && npm run test:db && npm run test:lan",
    "test:db": "./supabase/tests/run.sh",
    "test:lan": "./apps/lan/test.sh",
    "check": "node scripts/check.mjs",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "pg": "^8.13.0"
  }
}
